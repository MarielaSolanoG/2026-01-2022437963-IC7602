services:
  dns-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - DNS_REMOTE_SERVER=${DNS_REMOTE_SERVER}